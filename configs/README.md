# Example Configs

This directory contains some example configs for installing pjm for
various platforms. Package maintainers or people writing scripts to install pjm
may need to adopt (especially on windows!) if the default config does not work.
